package com.example.maplemedia.ui.mopub;


import android.app.Application;
import androidx.annotation.NonNull;
import androidx.lifecycle.AndroidViewModel;
import androidx.lifecycle.LiveData;
import androidx.lifecycle.MutableLiveData;

/**
 * MoPubViewModel
 * View model for the MoPub fragment.  Not used in this application but View state would be saved here
 * Included for future improvements.
 */
public class MopubViewModel extends AndroidViewModel {
    public MopubViewModel(@NonNull Application application) {
        super(application);
    }

}