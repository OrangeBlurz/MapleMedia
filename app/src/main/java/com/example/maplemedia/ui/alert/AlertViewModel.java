package com.example.maplemedia.ui.alert;

import android.app.Application;

import androidx.annotation.NonNull;
import androidx.lifecycle.AndroidViewModel;

/**
 * AlertViewModel
 * View model for the Alert fragment.  Not used in this application but View state would be saved here
 * Included for future improvements.
 */
public class AlertViewModel extends AndroidViewModel {

    public AlertViewModel(@NonNull Application application) {
        super(application);
    }
}